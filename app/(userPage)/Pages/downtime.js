'use client'; // Named export for 'use client'
import { Container } from 'react-bootstrap';

const Timetable = () => {
  return (
    <Container fluid className="p-6">
      <h1>Timetable</h1>
      {/* You can add your timetable content here later */}
    </Container>
  );
};

export default Timetable;
